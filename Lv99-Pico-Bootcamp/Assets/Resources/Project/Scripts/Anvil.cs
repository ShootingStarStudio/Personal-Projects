using UnityEngine;

public class Anvil : MonoBehaviour
{
    //check if player has hammer

    //get collider for anvil

    //check if there is an ingot on the anvil

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
